#include <iostream>
using namespace std;

int main() {
    int grade;
    do {
        cout << "Enter a grade (0-100): ";
        cin >> grade;
    } while (grade < 0 || grade > 100);

    cout << "Grade entered: " << grade << endl;
    return 0;
}

